extern int p;
int* fibonacci(int n)
{
	if (n == 1 || n == 0)
		return 1;
	return fibonacci(n-1) + fibonacci(n-2);
}
int main(int argc, char* argv)
{
	int i, k, a[10], b;
	int *p;
	i = + - - + 0;
	fibonacci(k);
	while (i++ < 10)
	{
		char c;
		*a;
		c = &i;
		b = fibonacci(b);
		a[k=i] = b++ + --i;
	}
	return 0;
}
