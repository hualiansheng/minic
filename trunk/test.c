extern int p;
int* fibonacci(int n)
{
	if (n == 1 || n == 0)
		return 1;
	return fibonacci(n-1) + fibonacci(n-2);
}
int main(int argc, char* argv)
{
	int i, k, a[10], b;
	i = + - - + 0;
	b = *x * *y;
	fibonacci(fibonacci(k));
	for(;;);
	while (i++ < 10)
	{
		char c;
		b = fibonacci(b);
		a[k=i] = b++ + --i;
		i = !(i && j);
	}
	return 0;
}
