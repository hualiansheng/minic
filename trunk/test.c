extern int p;
int f(int a,char b,int* c);


int fibonacci(int n)
{
	if (n == 1 || n == 0)
		return 1;
	return fibonacci(n-1) + fibonacci(n-2);
}

int main(int argc, char* argv)
{
	int i, k, a[10], b;
	int *p;
	int g(int a,int b,char* c);
	char w;
	g(i,i,i);
	i = + - - + 0;
	fibonacci(k);
	i + k;
	k(f(i,w,p));
	while (i++ < 10)
	{
		char c, k(char a);
		*a;
		c = &i;
		
		b = fibonacci(b);
		a[i] = b++ + --i;
	}
	return 0;
}
